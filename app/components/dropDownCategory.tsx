import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";

interface DropDownCategoryProps {
  category: string;
}

const DropDownCategory = async () => {
  return <DropdownMenu>DropDown</DropdownMenu>;
};

export default DropDownCategory;
