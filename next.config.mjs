/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: [
      "cdn.dummyjson.com",
      "localhost",
      "hips.hearstapps.com",
      "ui-avatars.com",
    ],
  },
};

export default nextConfig;
